import '../../{{app_name}}_exporter.dart';

class {{app_name.pascalCase()}}Settings extends ConsumerWidget {
  const {{app_name.pascalCase()}}Settings({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}