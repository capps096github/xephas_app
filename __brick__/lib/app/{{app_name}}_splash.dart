import '../{{app_name}}_exporter.dart';


class {{app_name.pascalCase()}}Splash extends StatelessWidget {
  const {{app_name.pascalCase()}}Splash({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement splash screen
    return Container();
  }
}